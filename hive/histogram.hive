create table twitter (user STRING, follower STRING) ROW FORMAT DELIMITED FIELDS TERMINATED BY '\t';

load data inpath '/twitter.net' into table twitter;
(local HDFS)

select followers, count(*) from (select count(*) followers from twitter group by user) c group by followers order by followers asc;

